#include <bits/stdc++.h>
using namespace std;
typedef long long ll;
int main(){
    ios::sync_with_stdio(0);
    cin.tie(0);
    cout.tie(0);
    int t;
    cin>>t;
    while (t--){
        ll n;
        cin>>n;
        string s;
        cout<<"mul 9"<<endl;
        cout.flush();
        cin>>s;
        cout<<"digit"<<endl;
        cout.flush();
        cin>>s;
        cout<<"digit"<<endl;
        cout.flush();
        cin>>s;
        cout<<"add "<<n-9<<endl;
        cout.flush();
        cin>>s;
        cout<<"!"<<endl;
        cout.flush();
        cin>>s;
    }
}