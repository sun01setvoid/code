link模拟栈，头部为顶，一直更新头
link 模拟队列要有头和尾
//注意指针时 返回地址(用来查找) 和 返回指针的地址(node**next)(用来修改)，不一样
//因为next指向的是下一个空间的地址，而不是自己的。(如果想找到自己的位置（且不能直接访问该元素的话），需要自己的地址)！！！！！！！！
//修改指针需注意（找到的是不是指针的位置呢，一种方法是标识符，另一种方法是地址，函数传参那么大概需要的是指针的地址了）
//也不一定是修改，还有其他情况时，要注意你需要的是哪个空间（从头开始插入，更新头时）
//综上:注意空间位置，指针的值并不是他所处于的空间的地址，而是他所指向的空间的地址

//用引用返回空间（采用别名）更简单
