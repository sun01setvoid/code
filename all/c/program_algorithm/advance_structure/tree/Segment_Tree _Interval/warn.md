//点集划分和块集划分
//如果用点编号块，那么与点集划分几乎没有区别，要注意最后一个位置
//否则，注意1.左右区间mid是否都要给和2.下到子树左右端点之间的要求
//普通建树O(n),dfn序O(nlogn)

//为什么需要懒标记？
    1.更新和查找的顺序不定,延迟更新保证时间复杂度仍为O(logn)  (实质是按需更新，存储更新)
    2.更新和查找的区间是任意的，操作不成对 
    (成对的话就没有需要（前一对所放的位置与后一对都一样），删除不可能去到未存在的区间)